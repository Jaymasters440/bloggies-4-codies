<div class="card text-center">
    <div class="card-body">
        <h1 class="card-title" id="title" name="{{id}}">{{title}}</h1>
        <p class="card-text"><small class="text-muted">{{user.name}}</small></p>
        <h2 class="card-text">{{textContent}}</h2>
    </div>

        <div class="card-footer">
            
            <p><small class="text-muted">{{creation_date}}</small></p>
        </div>
    
</div>
<br>

{{> comment-section}}