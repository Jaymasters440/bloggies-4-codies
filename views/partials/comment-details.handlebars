<div class="card">
    <div class="card-body">
        
        <div class="container">
            <div class="row justify-content-between">
                <h5 class="card-title col">{{user.name}}</h5>
                <p class="col-2">{{format_time date_created}}</p>
            </div>
            
        <p>{{content}}</p>
        </div>
        
    </div>
</div>